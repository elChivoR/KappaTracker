namespace KappaTracker
{
    /// <summary>Single source of truth for the mod version + a few shared constants.</summary>
    public static class ModInfo
    {
        public const string Version = "1.3.0";

        /// <summary>"Secure container Kappa" item template id.</summary>
        public const string KappaContainerTpl = "5c093ca986f7740a1867ab12";
    }
}
