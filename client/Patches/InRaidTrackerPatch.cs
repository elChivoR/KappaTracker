using HarmonyLib;

namespace KappaTracker.Client
{
    /// <summary>Stub — body added in Task 8. Tags task names in the in-raid objective tracker (HUD).</summary>
    internal static class InRaidTrackerPatch
    {
        public static void Patch(Harmony h) { }
    }
}
